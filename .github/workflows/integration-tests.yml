name: Integration Tests

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]

jobs:
  call-workflow:
    uses: ./.github/workflows/test-plugin.yml
    with:
        # [LINEINFO] Specify explicit locations when ops are combined (#2734)
        triton-ref: '9f4ad17c22b889d3804d1a1c38ed79b0e936e82'
        triton-shared-ref: ${{ github.ref }}
